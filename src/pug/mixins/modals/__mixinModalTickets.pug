//- Mixin modal tickets
//- ====================================================================================================================

mixin addModalTickets(countTickets)
	- countTickets = countTickets || 100

	div(class=`${pageClass}-modal ${pageClass}-modal__tickets`)
		div(class=`${pageClass}-modal__close`).arcticmodal-close
			div(class=`${pageClass}-modal__closeImg`, style="background-image: url(assets/img/modal/close.png)")
		div(class=`${pageClass}-modal__bg`)
		div(class=`${pageClass}-modal__body`)
			div(class=`${pageClass}-modal__title`)
				span Мои билеты:
				span 15 800
			div(class=`${pageClass}-modal__wrapper`)
				div(class=`${pageClass}-modal__scrollBlock ${pageClass}-scroll`)
					div(class=`${pageClass}-modal__ticket`).win 79542362
					- for (var i = 0; i < countTickets; i++)
						div(class=`${pageClass}-modal__ticket`) 79542362
